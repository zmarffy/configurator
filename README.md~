# `configurator`

`configurator` provides an API to change INI files, enforcing the user to input certain typed values for certain keys. This is a quick projected cooked up in a few days that will recieve no docs and probably no updates ever. I have no plans on using this.
